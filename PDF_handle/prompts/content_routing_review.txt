You are reviewing a single flagged content block for routing and preservation planning.

Rules:
- Return JSON only.
- Return exactly one JSON object that matches the requested schema.
- Do not wrap the response in markdown fences.
- Preserve good content whenever possible.
- Do not choose `drop` unless the material is junk, incoherent, duplicative, or source-dump residue.
- Choose `move_to_existing_entry` only when the target slug is clearly supported and is present in the supplied shortlist.
- Choose `keep_here_framed` only for material that can safely remain in the same entry as framed advanced content.
- Choose `move_to_library` for research, comparative, or foreign but valuable material that does not belong in the current teaching entry.
- Choose `create_future_entry_candidate` when the material is good but has no safe existing-entry destination and matches an allowed future-entry label.
- `target_slug` must be JSON null unless `routing_decision=move_to_existing_entry`.
- `future_entry_label` must be JSON null unless `routing_decision=create_future_entry_candidate`.
- `library_bucket` must be JSON null unless `routing_decision=move_to_library`.
- Use JSON null rather than the string "null".
- Prefer conservative routing over speculative existing-entry targeting.

Examples:

Clean example:
Input characteristics:
- clear existing-entry shortlist match
- coherent block
- no library/future route needed

Output:
{"routing_decision":"move_to_existing_entry","routing_confidence":"high","target_slug":"cable-tow","future_entry_label":null,"library_bucket":null,"explanation":"The block clearly belongs in the supplied existing-entry shortlist target."}

Ambiguous example:
Input characteristics:
- useful foreign or research material
- no safe existing-entry destination
- preservation-first fallback

Output:
{"routing_decision":"move_to_library","routing_confidence":"medium","target_slug":null,"future_entry_label":null,"library_bucket":"unassigned_research","explanation":"The block is worth preserving, but no safe existing-entry destination is supported by the shortlist."}
